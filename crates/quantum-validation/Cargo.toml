[package]
name = "quantum-validation"
version = "0.2.2"
license.workspace = true
authors.workspace = true
homepage.workspace = true
repository.workspace = true
edition.workspace = true

[dependencies]
blake3 = { version = "1.8.2", default-features = false }
codec = { workspace = true, features = ["derive", "max-encoded-len"] }
libm = { version = "0.2.15", default-features = false }
rand_chacha = { version = "0.9.0", default-features = false }
rand_core = { version = "0.9.3", default-features = false }
scale-info = { workspace = true, features = ["derive"] }
sp-core = { workspace = true }
thiserror = { version = "2.0.18", optional = true }

[features]
default = ["std"]
# Test/benchmark fixtures (e.g. `caterpillar_of_cliques`) that must NOT ship in
# the runtime blob. Enabled by `pallet-quantum-pow/runtime-benchmarks`, which is
# what makes them reachable from a no_std benchmark build.
runtime-benchmarks = []
std = [
    "blake3/std",
    "codec/std",
    "rand_chacha/std",
    "rand_core/std",
    "scale-info/std",
    "sp-core/std",
    "dep:thiserror",
]

[dev-dependencies]
hex = { version = "0.4.3" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = { version = "1.0.145" }
